{
  "name": "ancient-mixins",
  "version": "0.0.0",
  "description": "Core functions, classes, types and interfaces.",
  "bugs": "https://github.com/AncientSouls/Mixins/issues",
  "repository": "AncientSouls/Mixins",
  "license": "MIT",
  "scripts": {
    "tsc": "rm -rf ./lib ./tests && tsc ./src/*/*.ts --outDir ./ --sourceMap -d true --module CommonJS",
    "test": "mocha ./tests/index.js --require source-map-support/register",
    "dev-link": "true",
    "dev-init": "(rm -rf .dev && git clone -b dev https://github.com/AncientSouls/AncientSouls.github.io.git .dev && cd .dev && git submodule init && git submodule update && npm run dev-install) && npm link ./ && (cd .dev && npm run dev-use-links && npm run dev-check)"
  },
  "dependencies": {
    "events": "^2.0.0",
    "lodash": "^4.17.5",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.4.6",
    "chai": "^4.1.2",
    "mocha": "^5.0.1",
    "sinon": "^4.4.2",
    "source-map-support": "^0.5.3",
    "typescript": "^2.7.2"
  }
}
